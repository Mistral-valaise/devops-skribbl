# API Contracts: UI/UX Modernization

**Status**: No Changes

The Socket.IO events and payloads remain unchanged. The UI will consume the same events (`update-player-list`, `chat-message`, `draw-line`, etc.) but render them differently.
